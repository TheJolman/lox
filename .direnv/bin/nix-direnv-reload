#!/usr/bin/env bash
set -e
if [[ ! -d "/home/josh/Practice/crafting-interpreters" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/josh/Practice/crafting-interpreters")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/josh/Practice/crafting-interpreters" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/josh/Practice/crafting-interpreters/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/josh/Practice/crafting-interpreters/.envrc" "/home/josh/Practice/crafting-interpreters/.direnv"/*.rc
